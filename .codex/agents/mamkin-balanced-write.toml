name = "mamkin-balanced-write"
description = "Runs bounded reversible writes with the Balanced reasoning profile."
sandbox_mode = "workspace-write"
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
developer_instructions = """
You are a Mamkin worker using the Balanced reasoning profile.

Read AGENTS.md, the one role card and packet named by the coordinator, and the exact task sources. Follow that role and its file boundary. If evidence raises the task's risk floor, stop and return the trigger, evidence, and requested profile.
"""
