# Focused Policy Retest — 2026-09-08

## Outcome

Resume the user-requested Astra/medium coordinator pilot. Known packet failures cleared in staged focused tests. This is not a fresh full-suite execution result or real-project acceptance. Keep Deep/Critical Astra pilots and Terra Economy/Balanced unchanged; no effort reduction is supported.

## Round 1: Full Context, Complete Packets

Seven cases cover the four original omissions, adoption seed-only/approved-manual authority, and audit no-auto-apply behavior. Each fresh subagent receives the same richer synthetic facts and full relevant protocols. No word cap; no scenario execution. Expected answers are withheld.

| Arm | Passed | Remaining defects |
| --- | --- | --- |
| Sol/medium, pre-edit policy | 4/7 | Unsupported capability/evidence claims in three evolution packets |
| Astra/medium, pre-edit policy | 6/7 | Nonexistent worker role-card path |
| Astra/medium, revised policy | 6/7 | Same nonexistent role-card path |

Original omissions cleared even before the wording changes. This points to a context/format contribution; this sample does not demonstrate an isolated benefit from wording. Localized additions make the intended outputs explicit: formatter rejection prerequisites, recommendation approval, environment-variable names and sources without values, and complete worker assignments including escalation.

The corrected adoption oracle distinguishes seed-only approval from separately named manual changes. Protected template-owned collisions remain untouched in both. Current protocol—not the previous blanket oracle prohibition—grounds that distinction. Historical capped-run scores remain unchanged.

## Round 2: Resolve Real Source Paths

Both Astra packets guessed `docs/process/roles/worker.md`, which does not exist. Added a pre-dispatch current-file resolution rule and the canonical `docs/process/roles/implementation.md` mapping. Supplied the actual implementation role card and worker preset in the revised source inventory. Retested the balanced assignment plus a stale-role-path control.

| Arm | Packets | Rubric dimensions |
| --- | --- | --- |
| Sol/medium | 2/2 | 12/12 |
| Astra/medium, fresh run 1 | 2/2 | 12/12 |
| Astra/medium, fresh run 2 | 2/2 | 12/12 |

All correctly resolve the implementation role source, reject the stale path, preserve model/effort and evidence-triggered escalation, and retain independent acceptance. Instructions and source inventory changed together; their effects are not isolated.

## Evidence

- `manifest.json`: source commit, hashes, model/effort dispatch, labels, scope and limits.
- `before-policy.md`, `after-policy.md`, `final-policy.md`: immutable supplied policy snapshots.
- `inputs.json`, `oracle.json`, `worker-inputs.json`, `worker-oracle.json`: supplied facts and held-out acceptance expectations.
- Dispatch templates: exact common text with the per-arm absolute directory replaced by `<ARM_DIRECTORY>`; host/tool instructions are not reproduced.
- Six raw arm JSON files: participant packets, unchanged from local evaluation outputs.
- `round1-scores.json`, `worker-scores.json`: independent blinded grading. A/C are Astra pre/post; B is Sol pre; D is Sol worker control; E/F are Astra worker runs.
- `policy-review.json`: independent static policy review before the final source-resolution addition; that addition was assessed in worker scoring.

All packets are hypothetical intentions using synthetic facts. No adoption, worker edit, delegation, live steering, Figma action, external mutation, or Sambl migration occurred. Seven cases share each Round 1 context; Round 2 has only two Astra samples. Comparable cost, latency and tokens are unavailable. Accepted spawn configuration is the model evidence; separate runtime introspection is unavailable.

## Delivery And Next Step

The template's root instructions remain within the 750-word budget. Prompt contracts, the existing adoption tests and model-routing checks validate the applicable local structure; they do not convert these policy responses into executed workflow evidence.

Next is a separately scoped read-only sambl-design migration review, preserving its existing high effort and custom process. Publishing this template or approving its exact local-only source remains separate from migration apply. The target project is unchanged.
